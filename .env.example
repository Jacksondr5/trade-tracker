# App runtime
CLERK_SECRET_KEY=sk_test_your_clerk_secret_key
NEXT_PUBLIC_CLERK_FRONTEND_API_URL=https://your-clerk-frontend-api-url
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_your_clerk_publishable_key
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
VERCEL_GATEWAY_API_KEY=your_vercel_gateway_api_key

# AI counterpart (production Convex deployment)
# Clerk token identifier of the sole owner this token may access.
COUNTERPART_OWNER_ID=your-clerk-token-identifier
COUNTERPART_TOKEN=replace-with-a-long-random-shared-secret

# Bravos / Browserbase ingestion
# Set to true only in the production Convex deployment.
BRAVOS_SCHEDULED_SCANS_ENABLED=false
BRAVOS_WORKER_SECRET=replace-with-a-long-random-shared-secret
# Next.js worker route URL for Convex dispatch, for example:
# BRAVOS_WORKER_URL=http://127.0.0.1:3000/api/internal/bravos/run
BRAVOS_WORKER_URL=https://your-app.example.com/api/internal/bravos/run
BROWSERBASE_API_KEY=bb_live_your_browserbase_api_key
BROWSERBASE_PROJECT_ID=your_browserbase_project_id

# IBKR Flex Convex workflow
# Optional override; defaults to IBKR's documented Flex Web Service base URL.
IBKR_FLEX_BASE_URL=https://ndcdyn.interactivebrokers.com/AccountManagement/FlexWebService
# Base64-encoded 32-byte AES key, for example: openssl rand -base64 32
BROKERAGE_TOKEN_ENCRYPTION_KEY=your-base64-encoded-32-byte-key
BROKERAGE_TOKEN_ENCRYPTION_KEY_VERSION=1
# During rotation, retain older keys as BROKERAGE_TOKEN_ENCRYPTION_KEY_V1, etc.

# Playwright / Codex local UI testing
PLAYWRIGHT_PASSWORD=your-playwright-test-password
PLAYWRIGHT_OWNER_ID=your-clerk-token-identifier-for-the-playwright-user
PLAYWRIGHT_USERNAME=your-playwright-test-user@example.com
VERCEL_AUTOMATION_BYPASS_SECRET=your-vercel-automation-bypass-secret

# Comma-separated full Clerk/Convex tokenIdentifiers permitted to use this app.
# Keep this unset only in this example: an unset or empty value denies everyone.
ALLOWED_USER_IDS=

# Optional
# SKIP_ENV_VALIDATION=false
